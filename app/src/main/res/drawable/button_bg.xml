<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state (Darker for a click effect) -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="25dp"/>
            <solid android:color="#0047C0"/> <!-- Slightly darker blue -->
        </shape>
    </item>

    <!-- Focused state (For accessibility & keyboard navigation) -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="25dp"/>
            <solid android:color="#0066EE"/> <!-- Lighter blue for focus -->
        </shape>
    </item>

    <!-- Default state (Gradient with smooth transition) -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="25dp"/>
            <gradient
                android:type="linear"
                android:angle="90"
                android:startColor="#007BFF"
                android:centerColor="#0069E0"
                android:endColor="#0057D9"/>
            <stroke android:width="2dp" android:color="#0045B5"/> <!-- Subtle border -->
        </shape>
    </item>

</selector>
